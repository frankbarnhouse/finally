---
phase: 04-trading-terminal-frontend
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/components/Watchlist.tsx
  - frontend/src/components/MainChart.tsx
  - frontend/src/app/page.tsx
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Watchlist panel shows all watched tickers with live-updating prices"
    - "Price changes flash green (uptick) or red (downtick) fading over 500ms"
    - "Each watchlist ticker has a sparkline mini-chart that fills in progressively from SSE data"
    - "Clicking a ticker in the watchlist shows a larger price chart in the main chart area"
  artifacts:
    - path: "frontend/src/components/Watchlist.tsx"
      provides: "Watchlist with live prices, flash animations, sparklines"
      contains: "price-flash"
    - path: "frontend/src/components/MainChart.tsx"
      provides: "Large chart for selected ticker"
      contains: "createChart"
  key_links:
    - from: "frontend/src/components/Watchlist.tsx"
      to: "frontend/src/stores/priceStore.ts"
      via: "Zustand selectors for per-ticker price and history"
      pattern: "usePriceStore"
    - from: "frontend/src/components/MainChart.tsx"
      to: "frontend/src/stores/priceStore.ts"
      via: "Zustand selector for selected ticker history"
      pattern: "usePriceStore"
    - from: "frontend/src/components/Watchlist.tsx"
      to: "frontend/src/components/MainChart.tsx"
      via: "Selected ticker state"
      pattern: "selectedTicker"
---

<objective>
Build the Watchlist panel with live-updating prices, price flash animations, sparkline mini-charts, and the Main Chart area that shows the selected ticker's price history.

Purpose: Deliver the core market data visualization — the first thing users see when they open the terminal.
Output: Watchlist and MainChart components integrated into the page layout with live SSE data.
</objective>

<execution_context>
@/Users/frankbarnhouse/Projects/udemy_finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/frankbarnhouse/Projects/udemy_finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-trading-terminal-frontend/04-RESEARCH.md
@.planning/phases/04-trading-terminal-frontend/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/types/index.ts:
```typescript
export interface PriceUpdate {
  ticker: string;
  price: number;
  previous_price: number;
  timestamp: number;
  change: number;
  change_percent: number;
  direction: 'up' | 'down' | 'flat';
}
```

From src/stores/priceStore.ts:
```typescript
export const usePriceStore = create<PriceState>(...);
// State: prices (Record<string, PriceUpdate>), priceHistory (Record<string, {time, value}[]>), connectionStatus
```

From src/hooks/useLightweightChart.ts:
```typescript
export function useLightweightChart(containerRef: RefObject<HTMLDivElement | null>): { chartRef, seriesRef }
```

CSS classes in globals.css:
- .price-flash-up — green background with 0.5s fade
- .price-flash-down — red background with 0.5s fade
- .price-flash-none — transparent background with 0.5s fade
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Watchlist with live prices, flash animations, and sparklines</name>
  <files>
    frontend/src/components/Watchlist.tsx
  </files>
  <read_first>
    frontend/src/types/index.ts
    frontend/src/stores/priceStore.ts
    frontend/src/hooks/useLightweightChart.ts
    frontend/src/app/globals.css
    frontend/src/lib/api.ts
  </read_first>
  <action>
Create `src/components/Watchlist.tsx` as a "use client" component:

1. **Watchlist container:** Accepts `onSelectTicker: (ticker: string) => void` prop and `selectedTicker: string | null` prop. Fetches the watchlist on mount via `fetchWatchlist()` from api.ts to get the list of tickers.

2. **WatchlistRow sub-component** (defined in same file): Renders one ticker row. Uses Zustand selector for ONLY that ticker's price: `usePriceStore(s => s.prices[ticker])`. This ensures only the row for a changed ticker re-renders.

3. **Price display:** Format price to 2 decimal places. Show change and change_percent with + prefix for positive.

4. **Price flash animation:** Track the `direction` field from the PriceUpdate. When a new price arrives with direction 'up', apply CSS class `price-flash-up` to the price cell. When 'down', apply `price-flash-down`. After 500ms timeout, switch to `price-flash-none`. Use `useState` for flash state and `useEffect` keyed on the price value to trigger.

5. **Sparkline:** For each row, get `priceHistory[ticker]` from priceStore via selector. Use Lightweight Charts in a small container (roughly 100px wide, 30px tall). Configure the chart with NO axes, NO grid, NO crosshair, NO watermark — just a clean line. Use LineSeries with color #209dd7 (accent blue). Use the `useLightweightChart` hook from Plan 01. Call `series.setData()` when history changes — use `useEffect` keyed on priceHistory length. Chart time values must be in seconds (the SSE timestamp is already in seconds). Disable all chart interactions (handleScroll: false, handleScale: false). If sparklines at this small size look bad with Lightweight Charts, fall back to a simple inline SVG polyline mapping price values to points — this is acceptable and may be simpler.

6. **Row click:** Clicking a row calls `onSelectTicker(ticker)`. Highlight the selected row with a left border accent (border-l-2 border-accent-blue).

7. **Styling:** Dark theme — bg-terminal-surface, border-terminal-border. Ticker symbol in accent-yellow. Each row has subtle hover state. The component has a header "WATCHLIST" in muted text.

8. **Add/Remove:** Include a small input + "Add" button at the bottom for adding tickers (calls api.addToWatchlist then refreshes list). Each row has a small "x" button that calls api.removeFromWatchlist then refreshes list. Style the add button with bg-accent-purple.
  </action>
  <verify>
    <automated>cd /Users/frankbarnhouse/Projects/udemy_finally/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `Watchlist.tsx` contains `usePriceStore(s => s.prices[` (per-ticker selector)
    - `Watchlist.tsx` contains `price-flash-up` and `price-flash-down` CSS class references
    - `Watchlist.tsx` contains sparkline rendering (either `createChart` or `<svg`)
    - `Watchlist.tsx` contains `onSelectTicker` prop
    - `Watchlist.tsx` contains `fetchWatchlist` call
    - `Watchlist.tsx` contains `removeFromWatchlist` and `addToWatchlist` calls
    - Build succeeds
  </acceptance_criteria>
  <done>Watchlist panel displays live prices with flash animations and sparklines, supports adding/removing tickers, and communicates selected ticker</done>
</task>

<task type="auto">
  <name>Task 2: Main chart area and page layout integration</name>
  <files>
    frontend/src/components/MainChart.tsx
    frontend/src/app/page.tsx
  </files>
  <read_first>
    frontend/src/hooks/useLightweightChart.ts
    frontend/src/stores/priceStore.ts
    frontend/src/types/index.ts
    frontend/src/app/page.tsx
    frontend/src/components/Watchlist.tsx
  </read_first>
  <action>
1. Create `src/components/MainChart.tsx` as a "use client" component:
   - Accepts `ticker: string | null` prop
   - When ticker is null, show a muted message: "Select a ticker from the watchlist"
   - When ticker is set:
     a. Get price history from priceStore: `usePriceStore(s => s.priceHistory[ticker] || [])`
     b. Get current price: `usePriceStore(s => s.prices[ticker])`
     c. Use `useLightweightChart` hook with the container ref
     d. Create an AreaSeries (not LineSeries) for a more visual chart. Use `chart.addSeries(AreaSeries, {...})` with topColor rgba(32, 157, 215, 0.4), bottomColor rgba(32, 157, 215, 0.0), lineColor #209dd7
     e. Call `series.setData(history)` when history changes (useEffect on history length)
     f. For new individual data points, use `series.update(lastPoint)` for performance
     g. Show the ticker name and current price above the chart as a header
     h. Time axis: format as HH:MM (this is intraday data accumulated since page load)
   - Chart fills the container width and has a height of ~300-400px
   - Dark theme matching the terminal aesthetic

2. Update `src/app/page.tsx`:
   - Add `selectedTicker` state using useState (initially null, defaults to first ticker from watchlist)
   - Replace the watchlist placeholder with `<Watchlist onSelectTicker={setSelectedTicker} selectedTicker={selectedTicker} />`
   - Replace the main chart placeholder with `<MainChart ticker={selectedTicker} />`
   - Keep remaining placeholders for PositionsTable, PortfolioHeatmap, PnLChart, TradeBar, ChatPanel
   - Layout: The left column should have Watchlist at top-left, MainChart below it, and portfolio panels below that. The right column is the ChatPanel.
  </action>
  <verify>
    <automated>cd /Users/frankbarnhouse/Projects/udemy_finally/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `MainChart.tsx` contains `useLightweightChart` and `AreaSeries`
    - `MainChart.tsx` contains `usePriceStore` selector for priceHistory
    - `MainChart.tsx` handles null ticker with placeholder message
    - `page.tsx` imports and renders both `Watchlist` and `MainChart`
    - `page.tsx` contains `useState` for selectedTicker
    - Build succeeds
  </acceptance_criteria>
  <done>Main chart displays selected ticker's price history with area fill, watchlist and chart are wired together via selectedTicker state, page layout updated</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- `grep -r "price-flash" frontend/src/components/Watchlist.tsx` confirms flash animation classes
- `grep -r "AreaSeries" frontend/src/components/MainChart.tsx` confirms chart type
- `grep -r "selectedTicker" frontend/src/app/page.tsx` confirms wiring
</verification>

<success_criteria>
- Watchlist shows all tickers with live prices from SSE (prices update without full re-render)
- Price cells flash green/red on uptick/downtick, fading over 500ms
- Each ticker row has a sparkline showing accumulated price history since page load
- Clicking a ticker selects it and the main chart shows that ticker's price history as an area chart
- Users can add and remove tickers from the watchlist
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-trading-terminal-frontend/04-02-SUMMARY.md`
</output>
