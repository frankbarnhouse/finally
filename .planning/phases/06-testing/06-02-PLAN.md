---
phase: 06-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/__tests__/Watchlist.test.tsx
  - frontend/src/__tests__/PositionsTable.test.tsx
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "Frontend tests verify price flash CSS class changes on direction change"
    - "Frontend tests verify flash class resets after timeout"
    - "Frontend tests verify portfolio display calculates unrealized P&L percentage"
    - "Frontend tests verify positions table shows correct P&L color coding"
  artifacts:
    - path: "frontend/src/__tests__/Watchlist.test.tsx"
      provides: "Price flash animation timing tests"
      contains: "flash class resets"
    - path: "frontend/src/__tests__/PositionsTable.test.tsx"
      provides: "Portfolio display calculation tests"
      contains: "test_pnl_percentage"
  key_links:
    - from: "frontend/src/__tests__/Watchlist.test.tsx"
      to: "frontend/src/components/Watchlist.tsx"
      via: "component import"
      pattern: "import.*Watchlist"
    - from: "frontend/src/__tests__/PositionsTable.test.tsx"
      to: "frontend/src/components/PositionsTable.tsx"
      via: "component import"
      pattern: "import.*PositionsTable"
---

<objective>
Add missing frontend unit tests for price flash animation behavior (TEST-03) and portfolio display calculations.

Purpose: Existing frontend tests verify basic rendering and flash class presence. This plan adds tests for flash class timing behavior (set on price change, cleared after timeout) and portfolio P&L percentage calculations.

Output: Extended Watchlist.test.tsx and PositionsTable.test.tsx with additional test cases.
</objective>

<execution_context>
@/Users/frankbarnhouse/Projects/udemy_finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/frankbarnhouse/Projects/udemy_finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/__tests__/Watchlist.test.tsx
@frontend/src/__tests__/PositionsTable.test.tsx
@frontend/src/components/Watchlist.tsx
@frontend/src/components/PositionsTable.tsx
@frontend/src/test-utils.tsx
@frontend/src/setupTests.ts
@frontend/vitest.config.ts
</context>

<interfaces>
<!-- Key component contracts -->

From frontend/src/components/Watchlist.tsx:
- Props: { onSelectTicker: (ticker: string) => void; selectedTicker: string | null }
- Internal: uses usePriceStore for prices/priceHistory, useWatchlistStore for tickers
- Flash: useState("price-flash-none"), sets "price-flash-up"/"price-flash-down" on direction change, setTimeout 500ms to reset

From frontend/src/components/PositionsTable.tsx:
- Props: none (reads from usePortfolioStore and usePriceStore)
- Renders: ticker, quantity, avg cost, current price, unrealized P&L, % change
- CSS classes: text-profit (green), text-loss (red)

From frontend/src/stores/priceStore (mock shape):
- prices: Record<string, { ticker, price, previous_price, timestamp, change, change_percent, direction }>
- priceHistory: Record<string, { time: number, value: number }[]>
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add price flash animation and portfolio calculation tests</name>
  <files>frontend/src/__tests__/Watchlist.test.tsx, frontend/src/__tests__/PositionsTable.test.tsx</files>
  <read_first>
    - frontend/src/__tests__/Watchlist.test.tsx
    - frontend/src/__tests__/PositionsTable.test.tsx
    - frontend/src/components/Watchlist.tsx
    - frontend/src/components/PositionsTable.tsx
    - frontend/src/stores/priceStore.ts
    - frontend/src/stores/portfolioStore.ts
  </read_first>
  <behavior>
    Watchlist tests to add:
    - "displays green flash class for uptick direction": Verify "price-flash-up" class exists when direction is "up"
    - "displays red flash class for downtick direction": Verify "price-flash-down" class exists when direction is "down"
    - "shows price change percentage from SSE data": Verify change_percent values rendered in the watchlist rows

    PositionsTable tests to add:
    - "calculates and displays P&L percentage correctly": Position with avg_cost=150, current_price=190 shows +26.67% (green)
    - "displays negative P&L percentage correctly": Position with avg_cost=160, current_price=140 shows -12.50% (red)
    - "renders current prices from price store when available": Verify usePriceStore prices override position current_price
  </behavior>
  <action>
    PART A — Extend frontend/src/__tests__/Watchlist.test.tsx:

    Add these test cases inside the existing `describe("Watchlist")` block, after the existing tests. Use the same mock setup already in the file.

    1. "displays green flash class for uptick direction" — render Watchlist, find AAPL price element (191.00), verify its container has class containing "price-flash-up"
    2. "displays red flash class for downtick direction" — render Watchlist, find GOOGL price element (174.00), verify its container has class containing "price-flash-down"
    3. "shows price change values for each ticker" — render Watchlist, verify that change values from the mock price store data appear (e.g., the text "+0.26%" or "-0.71%" is present, OR the change value 0.50 / -1.25 text is rendered)

    PART B — Extend frontend/src/__tests__/PositionsTable.test.tsx:

    Add these test cases inside the existing `describe("PositionsTable")` block, after the existing tests.

    4. "calculates and displays P&L percentage" — Use positions mock with avg_cost=150, current_price=190. Verify the rendered percentage text shows approximately +26.67%
    5. "displays negative P&L percentage" — Use positions mock with avg_cost=160, current_price=140. Verify rendered percentage shows approximately -12.50%
    6. "renders current prices from price store" — Provide price store with AAPL price=195.00 (different from position current_price=190), verify 195.00 appears in rendered output

    IMPORTANT: Read the actual component source files first to understand exact rendering logic (what text format for percentages, what CSS classes, what store selectors). Match tests to actual component output.
  </action>
  <verify>
    <automated>cd /Users/frankbarnhouse/Projects/udemy_finally/frontend && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "flash class for uptick" frontend/src/__tests__/Watchlist.test.tsx returns 1
    - grep -c "flash class for downtick" frontend/src/__tests__/Watchlist.test.tsx returns 1
    - grep -c "P&L percentage" frontend/src/__tests__/PositionsTable.test.tsx returns at least 1
    - All frontend tests pass (exit code 0)
  </acceptance_criteria>
  <done>6 new frontend tests added: 3 for price flash animation classes, 3 for portfolio P&L display calculations. All pass alongside existing 18 tests.</done>
</task>

</tasks>

<verification>
All frontend tests pass:
```bash
cd frontend && npx vitest run --reporter=verbose
```
</verification>

<success_criteria>
- All existing 18 frontend tests continue to pass (no regressions)
- 6 new tests added covering flash animation CSS classes and P&L percentage display
- Tests verify both positive and negative P&L scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing/06-02-SUMMARY.md`
</output>
