---
phase: 06-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/services/test_portfolio.py
  - backend/tests/services/test_chat.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Backend tests verify fractional share trades execute correctly"
    - "Backend tests verify no-price-available ticker returns error"
    - "Backend tests verify invalid side parameter returns error"
    - "Backend tests verify LLM exception path returns graceful error"
    - "Backend tests verify ChatResponse parsing handles missing optional fields"
  artifacts:
    - path: "backend/tests/services/test_portfolio.py"
      provides: "Additional trade execution edge case tests"
      contains: "test_buy_fractional_shares"
    - path: "backend/tests/services/test_chat.py"
      provides: "LLM parsing and error handling tests"
      contains: "test_llm_exception_returns_error_message"
  key_links:
    - from: "backend/tests/services/test_portfolio.py"
      to: "backend/app/services/portfolio.py"
      via: "execute_trade import"
      pattern: "from app.services.portfolio import execute_trade"
    - from: "backend/tests/services/test_chat.py"
      to: "backend/app/services/chat.py"
      via: "handle_chat_message import"
      pattern: "from app.services.chat import handle_chat_message"
---

<objective>
Add missing backend unit tests for trade execution edge cases (TEST-01) and LLM structured output parsing (TEST-02).

Purpose: Fill coverage gaps in existing test suites. Tests for insufficient cash, insufficient shares, full position close, and basic chat mock are already present. This plan adds fractional shares, no-price ticker, invalid side, LLM exceptions, and ChatResponse parsing edge cases.

Output: Extended test_portfolio.py and test_chat.py with additional test functions.
</objective>

<execution_context>
@/Users/frankbarnhouse/Projects/udemy_finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/frankbarnhouse/Projects/udemy_finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/services/portfolio.py
@backend/app/services/chat.py
@backend/tests/services/test_portfolio.py
@backend/tests/services/test_chat.py
@backend/tests/conftest.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/services/portfolio.py:
```python
async def execute_trade(price_cache, ticker: str, side: str, quantity: float) -> tuple[dict | None, str | None]:
async def get_portfolio(price_cache) -> dict:
async def record_snapshot(price_cache) -> None:
async def get_portfolio_history() -> list[dict]:
```

From backend/app/services/chat.py:
```python
class TradeAction(BaseModel):
    ticker: str; side: str; quantity: float

class WatchlistChange(BaseModel):
    ticker: str; action: str  # "add" or "remove"

class ChatResponse(BaseModel):
    message: str
    trades: list[TradeAction] = Field(default_factory=list)
    watchlist_changes: list[WatchlistChange] = Field(default_factory=list)

async def handle_chat_message(user_message: str, price_cache, data_source) -> dict:
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trade execution edge case tests</name>
  <files>backend/tests/services/test_portfolio.py</files>
  <read_first>
    - backend/tests/services/test_portfolio.py
    - backend/app/services/portfolio.py
  </read_first>
  <behavior>
    - test_buy_fractional_shares: Buy 2.5 shares of AAPL at 190.50, verify position quantity=2.5, cash deducted by 2.5*190.50=476.25
    - test_sell_fractional_shares: Insert position qty=10.5, sell 3.5, verify remaining=7.0 and cash increased by 3.5*190.50
    - test_trade_no_price_available: Execute trade on ticker "UNKNOWN" (not in price_cache), verify error contains "No price available"
    - test_trade_invalid_side: Execute trade with side="short", verify error contains "Invalid side"
    - test_buy_exactly_all_cash: Set cash to exactly 1905.00 (10 * 190.50), buy 10 AAPL, verify succeeds with cash=0.0
    - test_sell_partial_then_close: Sell 5 of 10 shares, then sell remaining 5, verify first returns position, second returns position=None
  </behavior>
  <action>
    Append new test functions to the EXISTING backend/tests/services/test_portfolio.py file. Do NOT duplicate or modify any existing tests. Use the same db and price_cache fixtures already defined in the file. The MockPriceCache fixture has prices: {"AAPL": 190.50, "GOOGL": 175.00, "TSLA": 250.00}.

    Add these tests at the end of the file:
    1. test_buy_fractional_shares — buy 2.5 AAPL, assert quantity=2.5, cash deducted correctly
    2. test_sell_fractional_shares — insert position qty=10.5, sell 3.5, assert remaining=7.0
    3. test_trade_no_price_available — trade on "UNKNOWN", assert error "No price available"
    4. test_trade_invalid_side — trade with side="short", assert error "Invalid side"
    5. test_buy_exactly_all_cash — set cash=1905.00, buy 10 AAPL at 190.50, assert success with cash=0
    6. test_sell_partial_then_close — sell 5 of 10, then sell 5 more, verify position=None on second
  </action>
  <verify>
    <automated>cd /Users/frankbarnhouse/Projects/udemy_finally/backend && uv run --extra dev pytest tests/services/test_portfolio.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "test_buy_fractional_shares" backend/tests/services/test_portfolio.py returns 1
    - grep -c "test_sell_fractional_shares" backend/tests/services/test_portfolio.py returns 1
    - grep -c "test_trade_no_price_available" backend/tests/services/test_portfolio.py returns 1
    - grep -c "test_trade_invalid_side" backend/tests/services/test_portfolio.py returns 1
    - grep -c "test_buy_exactly_all_cash" backend/tests/services/test_portfolio.py returns 1
    - grep -c "test_sell_partial_then_close" backend/tests/services/test_portfolio.py returns 1
    - All tests in test_portfolio.py pass (exit code 0)
  </acceptance_criteria>
  <done>6 new edge case tests added and passing alongside existing 15 tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LLM structured output parsing tests</name>
  <files>backend/tests/services/test_chat.py</files>
  <read_first>
    - backend/tests/services/test_chat.py
    - backend/app/services/chat.py
  </read_first>
  <behavior>
    - test_chat_response_model_minimal: ChatResponse(message="hi") parses with empty trades and watchlist_changes
    - test_chat_response_model_with_actions: ChatResponse with trades and watchlist_changes parses correctly
    - test_chat_response_model_validate_json_minimal: ChatResponse.model_validate_json('{"message":"hi"}') succeeds with defaults
    - test_chat_response_model_validate_json_malformed: ChatResponse.model_validate_json("not json") raises ValidationError
    - test_llm_exception_returns_error_message: Monkeypatch LLM_MOCK=false and acompletion to raise, verify response contains "error"
    - test_mock_sell_triggers_trade: Send "sell some AAPL" with position in DB, verify sell trade action executed
  </behavior>
  <action>
    Append new test functions to the EXISTING backend/tests/services/test_chat.py file. Do NOT duplicate or modify any existing tests. Use the same db, price_cache, data_source, and mock_env fixtures already defined.

    Add these tests at the end of the file:
    1. test_chat_response_model_minimal — construct ChatResponse(message="hi"), assert trades==[] and watchlist_changes==[]
    2. test_chat_response_model_with_actions — construct ChatResponse with trades=[TradeAction(ticker="AAPL", side="buy", quantity=5)] and watchlist_changes=[WatchlistChange(ticker="PYPL", action="add")], assert lengths
    3. test_chat_response_model_validate_json_minimal — call ChatResponse.model_validate_json('{"message":"hi"}'), assert defaults
    4. test_chat_response_model_validate_json_malformed — call ChatResponse.model_validate_json("not json"), assert raises pydantic.ValidationError
    5. test_llm_exception_returns_error_message — monkeypatch LLM_MOCK to "false", monkeypatch litellm.acompletion to raise RuntimeError("API down"), call handle_chat_message, assert "error" in result["message"].lower()
    6. test_mock_sell_triggers_trade — insert AAPL position qty=10, send "sell some AAPL", verify position quantity decreased in DB

    For test 5, use monkeypatch to override LLM_MOCK env var to "false" and patch app.services.chat.acompletion to an async function that raises RuntimeError.
  </action>
  <verify>
    <automated>cd /Users/frankbarnhouse/Projects/udemy_finally/backend && uv run --extra dev pytest tests/services/test_chat.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "test_chat_response_model_minimal" backend/tests/services/test_chat.py returns 1
    - grep -c "test_chat_response_model_validate_json_malformed" backend/tests/services/test_chat.py returns 1
    - grep -c "test_llm_exception_returns_error_message" backend/tests/services/test_chat.py returns 1
    - grep -c "test_mock_sell_triggers_trade" backend/tests/services/test_chat.py returns 1
    - All tests in test_chat.py pass (exit code 0)
  </acceptance_criteria>
  <done>6 new LLM parsing and error handling tests added and passing alongside existing 10 tests</done>
</task>

</tasks>

<verification>
All backend tests pass:
```bash
cd backend && uv run --extra dev pytest tests/services/ -v
```
</verification>

<success_criteria>
- All existing 26 backend service tests continue to pass (no regressions)
- 12 new tests added and passing (6 portfolio edge cases + 6 chat parsing)
- Fractional shares, no-price ticker, invalid side, LLM exception, malformed JSON all covered
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing/06-01-SUMMARY.md`
</output>
