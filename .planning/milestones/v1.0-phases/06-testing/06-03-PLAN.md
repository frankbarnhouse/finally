---
phase: 06-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/docker-compose.test.yml
  - test/playwright.config.ts
  - test/package.json
  - test/tsconfig.json
  - test/tests/app.spec.ts
autonomous: true
requirements:
  - TEST-04

must_haves:
  truths:
    - "Playwright E2E tests run against the Docker container"
    - "Tests verify prices are streaming via SSE"
    - "Tests verify watchlist add and remove work"
    - "Tests verify buy and sell trades execute correctly"
    - "Tests verify chat interaction produces a response"
  artifacts:
    - path: "test/docker-compose.test.yml"
      provides: "Docker Compose config for E2E test environment"
      contains: "LLM_MOCK=true"
    - path: "test/playwright.config.ts"
      provides: "Playwright test configuration"
      contains: "baseURL"
    - path: "test/package.json"
      provides: "E2E test dependencies"
      contains: "@playwright/test"
    - path: "test/tests/app.spec.ts"
      provides: "Core E2E test scenarios"
      contains: "prices streaming"
  key_links:
    - from: "test/docker-compose.test.yml"
      to: "Dockerfile"
      via: "builds the app container"
      pattern: "build.*context"
    - from: "test/tests/app.spec.ts"
      to: "http://localhost:8000"
      via: "HTTP requests to running container"
      pattern: "localhost:8000"
---

<objective>
Create Playwright E2E test infrastructure and core test scenarios that run against the Docker container with LLM_MOCK=true.

Purpose: Verify the full application stack works end-to-end. Tests exercise the real backend, real database, and real frontend served from Docker, with only the LLM mocked for determinism.

Output: docker-compose.test.yml, Playwright config, package.json, and E2E test file covering core user flows.
</objective>

<execution_context>
@/Users/frankbarnhouse/Projects/udemy_finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/frankbarnhouse/Projects/udemy_finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@Dockerfile
@.env.example
</context>

<interfaces>
<!-- API endpoints the E2E tests will interact with -->

Backend API:
- GET /api/health — returns 200 with {"status": "ok"}
- GET /api/stream/prices — SSE stream, events have data with ticker/price/direction
- GET /api/watchlist — returns array of {ticker, price}
- POST /api/watchlist — body: {ticker: string}, returns added ticker
- DELETE /api/watchlist/{ticker} — removes ticker
- GET /api/portfolio — returns {positions, cash_balance, total_value}
- POST /api/portfolio/trade — body: {ticker, quantity, side}, returns {trade, cash_balance, position}
- POST /api/chat — body: {message: string}, returns {message, actions}

Frontend routes:
- / — main page, single-page app with all panels
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test infrastructure</name>
  <files>test/docker-compose.test.yml, test/playwright.config.ts, test/package.json, test/tsconfig.json</files>
  <read_first>
    - Dockerfile
    - .env.example
  </read_first>
  <action>
    Create 4 files in the test/ directory:

    1. **test/package.json** — Dependencies:
       ```json
       {
         "name": "finally-e2e",
         "private": true,
         "scripts": {
           "test": "playwright test",
           "test:headed": "playwright test --headed"
         },
         "devDependencies": {
           "@playwright/test": "^1.52.0",
           "typescript": "^5"
         }
       }
       ```

    2. **test/tsconfig.json** — Minimal TS config for Playwright:
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ESNext",
           "moduleResolution": "bundler",
           "strict": true
         }
       }
       ```

    3. **test/playwright.config.ts** — Configuration:
       - baseURL: "http://localhost:8000"
       - timeout: 30000 per test
       - retries: 1
       - use chromium only (no multi-browser needed)
       - webServer section is NOT used (Docker container started separately)
       - expect timeout: 10000

    4. **test/docker-compose.test.yml** — Docker Compose for test environment:
       - Service "app" that builds from ../Dockerfile (context: ..)
       - Ports: 8000:8000
       - Environment: LLM_MOCK=true, GOOGLE_API_KEY=test-key-not-used
       - No volume mount (fresh DB each run for clean state)
       - Healthcheck: curl http://localhost:8000/api/health with interval 5s, retries 10, start_period 30s

    After creating files, run `cd test && npm install && npx playwright install chromium` to install dependencies.
  </action>
  <verify>
    <automated>cd /Users/frankbarnhouse/Projects/udemy_finally/test && cat package.json | grep playwright && cat docker-compose.test.yml | grep LLM_MOCK && cat playwright.config.ts | grep baseURL</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "playwright" test/package.json returns at least 1
    - grep -c "LLM_MOCK=true" test/docker-compose.test.yml returns 1
    - grep -c "baseURL" test/playwright.config.ts returns 1
    - grep -c "chromium" test/playwright.config.ts returns at least 1
    - test/node_modules/@playwright/test exists after npm install
  </acceptance_criteria>
  <done>E2E test infrastructure ready: package.json with Playwright, docker-compose.test.yml with LLM_MOCK=true, Playwright config targeting localhost:8000</done>
</task>

<task type="auto">
  <name>Task 2: Write core E2E test scenarios</name>
  <files>test/tests/app.spec.ts</files>
  <read_first>
    - test/playwright.config.ts
    - backend/app/api/portfolio.py
    - backend/app/api/watchlist.py
    - backend/app/api/chat.py
  </read_first>
  <action>
    Create test/tests/app.spec.ts with these test scenarios. Tests interact with the API directly (not through the UI) for reliability, plus one page-level test.

    IMPORTANT: These tests are designed to run INDEPENDENTLY. Each test should work without depending on state from other tests. Use API calls for setup when needed.

    Test scenarios:

    1. **"health check returns ok"** — GET /api/health, expect 200 with status "ok"

    2. **"default watchlist has 10 tickers"** — GET /api/watchlist, expect 200, expect array length 10, expect AAPL in list

    3. **"prices are streaming via SSE"** — Use page.evaluate to open EventSource to /api/stream/prices, collect at least one event within 10 seconds, verify event data has ticker and price fields

    4. **"can add and remove ticker from watchlist"** — POST /api/watchlist with {ticker: "PYPL"}, verify 200. GET /api/watchlist, verify PYPL in list. DELETE /api/watchlist/PYPL, verify 200. GET /api/watchlist, verify PYPL NOT in list.

    5. **"can buy shares and see position"** — POST /api/portfolio/trade with {ticker: "AAPL", quantity: 5, side: "buy"}, verify 200, verify response has trade with ticker AAPL, verify cash_balance < 10000, verify position.quantity == 5. GET /api/portfolio, verify AAPL in positions.

    6. **"can sell shares"** — First buy 10 AAPL, then sell 5. Verify sell response has position.quantity == 5 and cash increased. Then sell remaining 5, verify position is null.

    7. **"chat returns AI response with mock"** — POST /api/chat with {message: "Tell me about my portfolio"}, verify 200, verify response has message string and actions object.

    8. **"page loads with trading terminal UI"** — Navigate to /, wait for page to load, verify page title or key elements exist (e.g., text "FinAlly" or watchlist ticker text "AAPL" appears within 15 seconds).

    Use Playwright's request context (page.request or test.use) for API calls. Group related assertions. Use test.describe("FinAlly E2E") wrapper.
  </action>
  <verify>
    <automated>cd /Users/frankbarnhouse/Projects/udemy_finally/test && npx playwright test --list 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "health check" test/tests/app.spec.ts returns 1
    - grep -c "prices are streaming" test/tests/app.spec.ts returns 1
    - grep -c "can add and remove ticker" test/tests/app.spec.ts returns 1
    - grep -c "can buy shares" test/tests/app.spec.ts returns 1
    - grep -c "can sell shares" test/tests/app.spec.ts returns 1
    - grep -c "chat returns AI response" test/tests/app.spec.ts returns 1
    - grep -c "page loads" test/tests/app.spec.ts returns 1
    - playwright test --list shows 8 tests
  </acceptance_criteria>
  <done>8 E2E test scenarios written covering health, watchlist CRUD, SSE streaming, buy/sell trades, chat interaction, and page load. Tests are listable by Playwright (actual execution requires Docker container running).</done>
</task>

</tasks>

<verification>
Test infrastructure validates:
```bash
cd test && npx playwright test --list
```

Full E2E run (requires Docker):
```bash
cd test && docker compose -f docker-compose.test.yml up -d --build --wait && npx playwright test && docker compose -f docker-compose.test.yml down
```
</verification>

<success_criteria>
- docker-compose.test.yml builds and runs the app container with LLM_MOCK=true
- Playwright config targets localhost:8000 with chromium
- 8 E2E test scenarios are defined and listable
- Tests cover: health, SSE streaming, watchlist CRUD, buy/sell, chat, page load
- When container is running, all 8 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing/06-03-SUMMARY.md`
</output>
